# Native C++ Test Skeleton

This directory contains the first-pass native C++ verification skeleton for:
- contract smoke tests
- manifest gate contracts
- staging path policy
- single-share / empty-slot import constraints

Real Windows/Qt/MSVC execution still needs to run on the target build machine.
